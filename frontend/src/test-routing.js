// Simple test to verify routing works
console.log("Testing routing setup...");

// This file is just to verify that the routing structure is correct
// The actual routing should work with the updated App.jsx file

const testRoutes = [
  "/",
  "/login", 
  "/register",
  "/lobby",
  "/game",
  "/game/123"
];

console.log("Routes configured:", testRoutes);
console.log("All routes should be accessible now");